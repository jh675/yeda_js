1. 깃 명령어 정리

 git add *                       => 커밋할 파일을 추가하는 명령어.
                                   ex) git add 파일명.확장자 (. 혹은 *은 수정된 모든 파일들을 커밋한다는 뜻.)
 git commit -m "12-17 숙제완료"   => 커밋 메세지
                                  ex) git commit -m "메세지 내용"
                                      (-m은 옵션. 메세지 내용은 넣지 않아도 무관.)
 git push origin main            => 커밋한 내용을 푸시하는 것.